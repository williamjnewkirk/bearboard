# React Native / Expo declare strict peer-dependency ranges that npm's default
# resolver rejects even for valid installs. Expo recommends legacy-peer-deps for
# npm-based projects. Keep this until the RN ecosystem's peer ranges relax.
legacy-peer-deps=true
